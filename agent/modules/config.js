// Replace with your deployed HTTPS backend before device testing.
// Empty means offline mode; the page never silently uses a fake live service.
export const SERVICE_URL = '';
// Enable only after Japanese speech has been verified on the target host.
export const JAPANESE_TTS_VERIFIED = false;
