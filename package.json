{
  "name": "mamori-japan-rokid",
  "version": "0.2.2",
  "private": true,
  "type": "module",
  "description": "まもり Japan disaster assistance for Rokid AIUI 0.17.0",
  "scripts": {
    "start": "python3 -m server.app",
    "test": "node --test tests/*.test.js && python3 -m unittest discover -s tests -p 'test_*.py'",
    "check": "node scripts/check-agent.mjs",
    "pack": "node scripts/pack.mjs"
  },
  "devDependencies": {
    "@yodaos-pkg/aix-cli": "0.8.2"
  }
}
